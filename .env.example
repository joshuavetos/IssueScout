# Server-only. Never prefix secrets with NEXT_PUBLIC_.
DATABASE_URL=postgresql://user:password@host:5432/database
APP_BASE_URL=http://localhost:3000
ALLOWED_GITHUB_USER_ID=211133238
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# 32-byte key encoded as base64. Example generator: openssl rand -base64 32
TOKEN_ENCRYPTION_KEY=
# 32+ random bytes encoded as base64 for session hashes/CSRF state.
SESSION_SECRET=
